# SQLite — geliştirme veritabanı (ROADMAP.md Faz 9'a kadar bu şekilde kalacak).
# Yol proje köküne göre çözülür: hem Prisma CLI hem Next.js kökten çalışır.
DATABASE_URL="file:./prisma/dev.db"

# Auth.js — üretimde mutlaka değiştirin: `npx auth secret` veya `openssl rand -base64 32`
AUTH_SECRET=""
